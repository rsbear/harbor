import { tv } from "tailwind-variants";

export const fullWidthHR = tv({
  base: "w-full bg-purple-500 border-transparent h-[1px]",
});

export const commandContainer = tv({
  base: "mt-auto shrink flex items-center divide-x divide-purple-600",
});
